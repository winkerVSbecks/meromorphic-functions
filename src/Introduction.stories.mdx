import { Meta } from '@storybook/addon-docs';
import { Tweet } from 'mdx-embed';

<Meta title="Introduction" />

# Meromorphic Functions

A tool for generating static and animated mesh gradients using a [meromorphic function](https://en.wikipedia.org/wiki/Meromorphic_function).

<div
  style={{
    backgroundImage: 'url("demo.png")',
    backgroundSize: 'cover',
    height: 200,
    backgroundPosition: '50% 75%',
    marginBottom: 32,
  }}
></div>

### Usage

Each visualization is generated using 4 points `(x, y)` and a color palette. You can chose between a static or animated visualization. Then use the controls to adjust the parameters:

- Color palette
- Color mode
- Location of the points (static visualization only)
- Motion type (animated visualization only)

🔁 Refresh the page to generate new random parameters.

#### Exporting images

- <kbd className="key">spacebar</kbd> to pause or resume the animation

- <kbd>
    <kbd className="key">cmd</kbd> + <kbd className="key">s</kbd>
  </kbd> to export the image

- <kbd>
    <kbd className="key">cmd</kbd> + <kbd className="key">shift</kbd> +{' '}
    <kbd className="key">s</kbd>
  </kbd> to export an image sequence of the animation. Then use <a href="https://ffmpeg.org/">
    ffmpeg
  </a> to convert it into a GIF or a video.

### How does it work?

The gradients are generated by visualizing the ratio of two complex polynomials using GLSL and [canvas-sketch](https://github.com/mattdesl/canvas-sketch). The underlying shader is based on Harley Turan's <a href="https://hturan.com/writing/complex-numbers-glsl">Visualizing Complex Numbers Using GLSL</a>.

<Tweet tweetLink="keenanisalive/status/1448011475231117315" hideConversation />
